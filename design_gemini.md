# Kitchen Log 디자인 시스템 가이드라인 (Design System Guidelines)

본 가이드라인은 카페24(Cafe24) 쇼핑몰 스킨의 CSS 파일 구조(`layout.css`, `main.css`, `sub_style.css` 등)와 메인 비주얼 분석을 바탕으로 정립된 **'Kitchen Log(키친로그)'**의 공식 디자인 시스템 문서입니다. 

본 문서는 **노트북LM(NotebookLM)** 및 AI 가이디드 시스템에서 브랜딩 아이덴티티 시각화, 콘셉트 조율, UI/UX 확장 설계 시 핵심 레퍼런스로 활용할 수 있도록 마크다운(Markdown) 형태로 상세히 구조화되었습니다.

---

## 1. 브랜드 및 디자인 콘셉트 (Brand Identity & Core Concept)

### 1.1 핵심 타깃 프로필 (Target Audience)
* **주요 타깃:** 2~4인 가구를 구성하고 있는 **3040 맞벌이 주부**.
* **라이프스타일 및 니즈:**
    * **평일 (실용성 중심):** 바쁜 일상 속에서 세척과 관리가 용이하고 사용이 극도로 편리한 프리미엄 실리콘 주방용품을 선호합니다.
    * **주말 (감성 및 미학 중심):** 가족을 위해 정성스러운 브런치를 요리하고, 이를 예쁘게 플레이팅하여 사진으로 기록(Log) 및 공유(SNS)하고 싶어하는 감성적 미학을 추구합니다.

### 1.2 디자인 키워드 (Design Keywords)
* **미니멀 (Minimal):** 불필요한 장식을 배제하고 선과 면, 여백의 미를 극대화하여 제품 자체의 본질을 돋보이게 합니다.
* **모던 (Modern):** 정돈된 그리드와 차분하고 정갈한 산세리프 타이포그래피를 사용하여 현대적인 세련미를 전달합니다.
* **내추럴 & 프랙티컬 (Natural & Practical):** 인위적이지 않은 뉴트럴/우드 톤을 베이스로 삼아 따뜻함을 더하고, 일상에서 가장 직관적이고 실용적으로 작동하는 UI 인터랙션을 구현합니다.

---

## 2. 색상 시스템 (Color Palette)

전체적인 색상은 따뜻한 우드 및 베이지 톤을 주축으로 하며, 눈의 피로감을 덜어주는 소프트 차콜과 유기적인 느낌의 오가닉 그린을 변형 테마로 두어 주방 환경 및 플레이팅 콘텐츠와의 완벽한 조화를 이룹니다.

### 2.1 메인 및 포인트 컬러 (Primary & Accent Colors)
* **Primary Accent (`#d0ac88`):** Warm Beige / Wood Brown
    * *적용 영역:* 최상단 배너 배경, 활성화된 메뉴 하단 언더라인, 스와이퍼 페이지네이션, 주요 구매 버튼, 우측 퀵배너 스크롤 탑 버튼 배경.
    * *의도:* 주방의 따뜻함과 원목 식기가 주는 아늑함, 그리고 주말 브런치 플레이팅의 감성을 직관적으로 대변합니다.
* **Primary Hover / Active (`#ac5600`):** Deep Orange Brown
    * *적용 영역:* 테두리 형태의 일반 버튼(`[class^='btnNormal']`) 마우스 호버(Hover) 또는 활성화 상태.
    * *의도:* 메인 베이지 톤보다 명도를 낮추어 시각적 피드백을 확실하게 제공합니다.

### 2.2 테마 확장 컬러 (Alternative Theme: Theme 02)
* **Theme 02 Accent (`#9fa581`):** Thistle Green (시슬 그린)
    * *적용 영역:* 서브 페이지 테마 전환 시 버튼 배경, 최상단 배너, 상품 할인율 뱃지 배경.
    * *의도:* 실리콘 소재 주방도구가 가진 친환경적이고 안전하며 실용적인 감각을 대변하는 톤다운된 그린 컬러입니다.
* **Theme 02 Hover (`#4d5c04`):** Deep Olive Green
    * *적용 영역:* 그린 테마 내 버튼 호버 시 테두리 컬러.

### 2.3 텍스트 컬러 (Typography Colors)
* **기본 본문 및 주요 타이틀 (`#1a1a1a`):** Soft Charcoal Black
    * *의도:* 완전한 블랙(`#000`)에 비해 대비가 부드러워 긴 텍스트나 고해상도 모니터에서도 고급스럽고 가독성이 높습니다.
* **보조 및 뮤트 텍스트 (`#888888`, `#777777`, `#666666`):** Slate Grays
    * *적용 영역:* 상품 스펙 설명, 카테고리 경로(Breadcrumb), 푸터 쇼핑몰 정보, 비활성 탭 메뉴.

### 2.4 배경 컬러 (Background Colors)
* **기본 배경 (`#ffffff`):** Pure White (메인 레이아웃 및 상품 리스트 배경)
* **서브 구획 배경 (`#f9f9f9`):** Light Gray (푸터 구역 전체, 장바구니 옵션 박스 내부 배경)
* **텍스트 배너 포인트 배경 (`#faf7f3`):** Warm Ivory (메인 페이지 롱 배너 구역에 적용하여 부드럽고 미니멀한 무드 전환 유도)
* **테이블 및 스크롤 바 내부 배경 (`#eeefe8`, `#dbd9cc`):** Muted Sand (디테일한 UI 컴포넌트의 깊이감을 더하기 위한 보조 음영 레이어)

---

## 3. 타이포그래피 (Typography)

고딕 기반의 정갈한 서체 배치로 모던함을 유지하되, 감성 중심의 서브 테마 진입 시 클래식한 명조 서체를 혼용할 수 있도록 유연하게 설계되었습니다.

### 3.1 폰트 스택 (Font Family)
* **기본 시스템 (Default Base):** `"Jost", "Noto Sans KR", Arial, sans-serif !important;`
    * 글로벌 감성의 모던 고딕 서체인 Jost와 한글 가독성이 뛰어난 Noto Sans KR을 매칭하여 깔끔하고 미니멀한 UI를 형성합니다.
* **세리프 테마 시스템 (Theme 02 Serif):** `"Lora", "Nanum Myeongjo", Arial, sans-serif !important;`
    * 클래식하고 서정적인 톤의 Lora와 나눔명조를 결합하여 감성적인 주방 라이프스타일 매거진과 같은 느낌을 연출합니다.

### 3.2 폰트 계층 구조 (Font Hierarchy - PC 기준)
* **H1 (메인 헤드라인 / 대타이틀):** `30pt` ~ `32pt`
    * *속성:* `font-weight: 500` 또는 `700`, `line-height: 1`
    * *적용 영역:* 메인 섹션 타이틀, 서브페이지 상단 타이틀(`.titleArea h2`)
* **H2 (서브 헤드라인 / 섹션 타이틀):** `24pt` ~ `26pt`
    * *속성:* `font-weight: 500`, `line-height: 1.2`
    * *적용 영역:* 이미지&텍스트 배너 타이틀, 비디오 배너 타이틀
* **GNB / 카테고리 메뉴:** `16pt`
    * *속성:* `font-weight: 400`, 메인 카테고리 링크에 적용
* **본문 / 상품 정보 텍스트 (Base):** `13pt` ~ `14pt`
    * *속성:* `font-weight: 300` (기본값) 또는 `400`, `line-height: 1.5` ~ `1.8`
    * *적용 영역:* 상품명, 가격, 상세 스펙 리스트, 푸터 정보

---

## 4. 반응형 레이아웃 및 그리드 (Responsive Layout & Grid)

### 4.1 디바이스별 분기점 (Breakpoints)
* **PC (Desktop):** `min-width: 1025px`
* **Tablet (태블릿):** `max-width: 1024px` (세부 스케일 구간: `768px` ~ `1024px`)
* **Mobile (모바일):** `max-width: 767px`

### 4.2 컨테이너 구조 (Container Specification)
* **기본 너비 사양:** 최대 너비 고정 방식(`max-width: 1480px`)을 채택하고 좌우 여백을 유연하게 부여(`width: 92%; margin: 0 auto;`)하여 화면 해상도가 커져도 레이아웃이 깨지지 않는 컴팩트한 미니멀리즘 구조를 유지합니다.
* **전체 화면 확장 레이아웃:** 비주얼 콘텐츠나 와이드 배너의 경우 `.section_full` 클래스를 통해 `width: 100% !important; max-width: 100% !important;`로 유연하게 확장됩니다.

### 4.3 상품 진열 그리드 시스템 (Product List Grid)
데스크톱 환경에서의 균형 잡힌 열 배치가 모바일 환경에서는 스와이프 슬라이더 혹은 축소형 레이아웃으로 유연하게 변형됩니다.

| 그리드 타입 | 데스크톱 너비 (PC) | 모바일 변형 방식 (Mobile) | 주요 활용 영역 |
| :--- | :--- | :--- | :--- |
| **5단 그리드 (`.grid5_slide`)** | 1열당 **20%** 배분 | 좌우 스크롤 스와이프 형태 (`width: calc(29% - 10px)`) | 신상품 카테고리, 다량 진열 영역 |
| **4단 그리드 (`.grid4_slide`)** | 1열당 **25%** 배분 | 좌우 스크롤 스와이프 형태 (`width: calc(40% - 10px)`) | 메인 추천 상품 리스트 |
| **3단 그리드 (`.grid3_slide`)** | 1열당 **33.33%** 배분 | 1열 세로 적층 배치 구조로 100% 채움 | 기획전, 포커스 상품 진열 |
| **2단 그리드 (`.grid2_slide`)** | 1열당 **50%** 배분 | 세로 적층 혹은 비율 유지 축소 | 시그니처 대형 배너형 상품 진열 |

---

## 5. 여백 및 간격 시스템 (Spacing & Vertical Rhythm)

'Kitchen Log'의 미니멀 무드를 완성하는 핵심은 **'과감한 수직 여백(Vertical Breathing Room)'**입니다. 각 기능적 섹션 간의 여백을 넓게 확보하여 제품의 프리미엄 이미지를 구축합니다.

### 5.1 섹션 간 하단 여백 (`margin-bottom` / `padding-bottom`)
* **PC 환경:** `120px`
    * *의도:* 정보의 밀도를 낮춰 화면을 잡지(Magazine) 화보처럼 여유롭게 감상할 수 있는 시각적 리듬을 부여합니다.
* **Tablet 환경:** `80px`
    * *의도:* 스크롤 면적이 좁아지는 환경에 맞추어 정보 간격을 소폭 압축합니다.
* **Mobile 환경:** `50px`
    * *의도:* 한 화면에 유의미한 정보 덩어리가 최소 1개 이상 노출되도록 간격을 최소화하되 미니멀 감성을 해치지 않는 한계선입니다.

### 5.2 컴포넌트 내부 세부 여백
* 최상단 띠배너 상하 패딩: `PC 20px` $ightarrow$ `Mobile 10px`
* 상품 카드 내 이미지와 하단 텍스트 정보 간격: `30px` 정밀 고정 (`.description { margin: 30px 0 0 0; }`)

---

## 6. UI 컴포넌트 및 인터랙션 요소를 위한 스타일 가이드 (UI Components & Interaction)

### 6.1 버튼 스타일 (Button Architecture)
* **제출형/메인 버튼 (`.btnSubmit`):**
    * Solid 스타일로 포인트 컬러 배경(`#d0ac88`)에 흰색 텍스트(`#fff`) 조합. 직관적이고 묵직한 사용감을 제공합니다.
* **일반형/서브 버튼 (`.btnNormal`):**
    * 테두리 라인 스타일로 포인트 컬러 선(`1px solid #d0ac88`)과 텍스트 컬러 매칭. 마우스 호버 시 다크 오렌지 브라운 테두리(`border-color: #ac5600`)로 부드럽게 전환됩니다.
* **크기 규격:**
    * `sizeL`: 높이 `56px`, 폰트 `16px` (로그인, 회원가입, 결제 등 핵심 전환 버튼)
    * `sizeM`: 높이 `50px`, 폰트 `14px` (일반 액션 버튼)

### 6.2 마크/뱃지 및 할인율 표시 (Badge Systems)
* **원형 플로팅 할인율 뱃지 (`.sale_box`):**
    * *스타일:* `position: absolute; border-radius: 50%; width: 60px; height: 60px;` 형태로 이미지 우측 상단에 오버레이 배치됩니다. 배경은 `#d0ac88`을 적용하여 시선을 모읍니다.
* **추천 상품 뱃지 (`.thumbnail .badge`):**
    * 이탤릭체 스타일(`font-style: italic; font-weight: 500; letter-spacing: 1px;`)을 사용하여 상업적 느낌을 덜어낸 고급스러운 가이드 라인을 형성합니다.

### 6.3 폼 요소 커스텀 (Custom Checkbox)
* **체크박스 (`input[type="checkbox"]`):**
    * 브라우저 기본 체크박스 UI를 전면 차단하고, 미니멀한 라인 형태의 맞춤형 SVG 그래픽 파일(`checkbox_off.svg`, `checkbox_on.svg`)을 연동하여 인터랙션 일관성을 높였습니다. (`width: 24px; height: 24px; transition: none;`)

### 6.4 상품 카드 호버 효과 (Product Card Hover Interactions)
* **강조형 갤러리 리스트 (`.list_gallery`):**
    * 평소에는 상품 정보 텍스트 구역의 불투명도가 0이며 위치가 하단에 숨겨져 있으나, 마우스 호버 시 `all 0.3s cubic-bezier` 트랜지션을 일으키며 불투명 반투명 블랙 레이어(`background-color: rgba(1, 1, 1, 0.5)`)와 함께 부드럽게 위로 미끄러지듯 올라와 정보가 노출됩니다. 주말 브런치 감성의 완성도 높은 이미지 컷을 해치지 않기 위한 미니멀리즘적 제어 방식입니다.